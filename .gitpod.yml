
tasks:
  - name: Terminal 1
    before: echo "Terminal 1"
    init: npm install && pip install -r requirements.txt




vscode:
  extensions:
    - ms-azuretools.vscode-docker
    - eamodio.gitlens
    - vscodevim.vim
# test gitpod workspace with github issue #1
# test some change issue2
